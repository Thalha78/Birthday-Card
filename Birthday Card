<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Birthday Card</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #ffebf0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .card {
            background-color: #f8c8d4;
            padding: 50px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
            width: 300px;
            position: relative;
        }

        .card h1 {
            font-size: 2.5rem;
            color: #e91e63;
            margin-bottom: 20px;
        }

        .card .hearts {
            font-size: 3rem;
            color: #ff4081;
        }

        .card button {
            background-color: #e91e63;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }

        .card button:hover {
            background-color: #d81b60;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>Happy Birthday!</h1>
        <div class="hearts">❤️❤️❤️</div>
        <button onclick="window.close()">Close</button>
    </div>

    <script>
        // Optional: To prevent the page from being closed immediately
        window.onload = function() {
            setTimeout(function() {
                alert("Make a wish!");
            }, 2000);  // Shows an alert after 2 seconds
        };
    </script>
</body>
</html>
